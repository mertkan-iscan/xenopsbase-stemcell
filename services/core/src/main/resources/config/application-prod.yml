# ===================================================================
# Spring Boot configuration for the "prod" profile.
#
# This configuration overrides the application.yml file.
#
# More information on profiles: https://www.jhipster.tech/profiles/
# More information on configuration properties: https://www.jhipster.tech/common-application-properties/
# ===================================================================

# ===================================================================
# Standard Spring Boot properties.
# Full reference is available at:
# https://docs.spring.io/spring-boot/appendix/application-properties/index.html
# ===================================================================

logging:
  level:
    ROOT: INFO
    tech.jhipster: INFO
    com.xenopsoftware.core: INFO

management:
  # THE SWITCH THAT MADE THE WHOLE PIPELINE EMPTY (T-2.12, #155).
  #
  # This read `false` until 2026-08-23. It disables the Micrometer Prometheus
  # registry, so `/management/prometheus` answered 404 even though `prometheus`
  # is listed in the exposure include list -- and no jvm_*, http_server_* or
  # pool metric from either service reached anywhere. Prometheus held 2423
  # metric names and none of them came from an application.
  #
  # The comment below the OTLP switch is what let it survive: it said metrics
  # already reached Prometheus another way. There was no other way.
  prometheus:
    metrics:
      export:
        enabled: true
  # Distributed tracing over OTLP (T-2.7).
  #
  # This block previously configured Zipkin at http://localhost:9411, paired
  # with a `zipkin` Maven profile that was never activated. The result was a
  # service that produced no spans at all, while Tempo and Alloy were deployed,
  # healthy and receiving nothing -- Tempo's own API answered {"traces":[]}.
  #
  # The endpoint has NO default on purpose. Spring only registers an OTLP
  # exporter when one is set, so a laptop with no collector generates spans and
  # exports nothing, rather than retrying against a port that is not listening.
  # The deployment supplies it; see platform/envs/dev/services/.
  #
  # TRACES go over OTLP. METRICS do not, and are scraped instead -- by the
  # ServiceMonitor in platform/envs/dev/services/servicemonitor.yaml, which is
  # the "another way" this comment used to assert without one existing.
  #
  # spring-boot-starter-opentelemetry brings micrometer-registry-otlp, which
  # turns OTLP metrics export on by default and aims it at
  # http://localhost:4318/v1/metrics. Nothing listens there and Alloy's
  # pipeline handles traces only, so leaving it on produced a stack trace once
  # a minute in every pod. Disabling it was right; believing that left a
  # working metrics path behind was not.
  #
  # Exactly one path is enabled. If OTLP metrics are ever turned on, the scrape
  # must be turned off in the same commit, or every series is counted twice.
  otlp:
    metrics:
      export:
        enabled: false
  opentelemetry:
    tracing:
      export:
        otlp:
          # NO `endpoint:` HERE, DELIBERATELY, AND THE OMISSION IS THE FIX
          # (T-2.16, #211).
          #
          # This read `endpoint: ${OTLP_TRACING_ENDPOINT:}`. The empty default
          # is a SET value, so Boot built the exporter and it rejected it:
          #
          #   IllegalArgumentException: Invalid endpoint, must start with
          #   http:// or https://:
          #
          # meaning the prod profile could not start anywhere the variable was
          # absent. In the cluster it never is, so the failure only appeared the
          # first time anything ran this profile elsewhere -- a local k3s
          # rehearsal of the HPA (T-2.8), where the gateway crash-looped.
          #
          # T-4.1 fixed exactly this line in application-dev.yml and it was
          # never carried across. Same shape as AuthoritiesConstants in T-3.20.
          #
          # The deployment now sets the real property directly, by its own name:
          #   MANAGEMENT_OPENTELEMETRY_TRACING_EXPORT_OTLP_ENDPOINT
          # so an absent variable leaves the property absent, no exporter is
          # registered, and the service starts with tracing off. Which is what
          # the comment here always claimed and never did.
          #
          # gRPC, not the `http` default: the Alloy receiver in this platform
          # listens on 4317 with a grpc block only. Sending protobuf-over-HTTP
          # to a gRPC port fails per export, in the exporter's own log, while
          # the application stays healthy.
          transport: grpc
  tracing:
    sampling:
      # ---------------------------------------------------------------------
      # 0.1, AND THE COMMENT THAT USED TO BE HERE WAS THE WARNING (T-5.12)
      # ---------------------------------------------------------------------
      #
      # This said "everything, which is right for dev and wrong for a busy
      # production service -- overridden by TRACING_SAMPLE_PROBABILITY rather
      # than by editing this". Nothing ever overrode it, and the prod profile
      # is what dev runs, so every request in every load test created spans in
      # both services.
      #
      # What that cost is not what T-5.10 looked for. It A/B-tested 1.0 against
      # 0.01 and found the difference INSIDE the services was noise -- 4.85m vs
      # 5.08m CPU per request -- and concluded tracing was not where the cost
      # went. The measurement was correct and the conclusion was too narrow: it
      # measured the emitters and never the collectors.
      #
      # T-5.12 measured the collectors. Under the same load, on the same nodes:
      #
      #   alloy       993m-1197m per pod, against a 50m request
      #   tempo       ~1650m, against a 100m request, and OOMKilled 16 times
      #
      # Tempo was never Ready for the whole of T-5.10 and T-5.11, so not one
      # span was ever stored. The tracing was not merely expensive, it was
      # expensive AND absent -- the worst available combination, and it took
      # the telemetry needed to diagnose the load tests down with it.
      #
      # 0.1 is Boot's own default and one trace in ten at 1000 req/s is still a
      # hundred a second, which is not a sampling problem for anything this
      # project needs to see. The env var still overrides, and it is still the
      # right way to raise it for a specific investigation.
      probability: ${TRACING_SAMPLE_PROBABILITY:0.1}

spring:
  devtools:
    restart:
      enabled: false
    livereload:
      enabled: false
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:postgresql://localhost:5432/core
    hikari:
      poolName: Hikari
      auto-commit: false
      # Every wait bounded (T-3.9). Hikari's own defaults are mostly sane; they
      # are restated because an unset value here is invisible, and the one that
      # matters most has no default at all -- see the statement timeout below.
      #
      # How long a thread waits for a connection from the pool before failing.
      # Failing fast is the point: a request that cannot get a connection within
      # a few seconds has already lost, and waiting only adds another stuck
      # thread to the pile.
      connection-timeout: 5000
      validation-timeout: 3000
      # Below the typical 15-minute idle timeout of a cloud load balancer or NAT,
      # so the pool discards a connection before the network silently does.
      # Otherwise the first query on a reused connection hangs until the socket
      # timeout, long after the request that owned it gave up.
      max-lifetime: 600000
      idle-timeout: 300000
      # SIZED AGAINST THE DATABASE, NOT AGAINST THIS SERVICE (T-2.18, #259).
      #
      # This number is multiplied by the HPA's maxReplicas. At 20 and three
      # replicas that is 60 of the database's 100 connections, from core alone,
      # before Keycloak or CNPG's own. The pool is not a local decision.
      #
      # The budget is asserted by infra/scripts/check-connection-budget.sh,
      # which fails when this, the HPA ceiling and max_connections stop adding
      # up. Change this and the check tells you what else has to move.
      #
      # 12 is generous for the work core does: measured usage is 5 per pod at
      # idle, queries are short, and connection-timeout below bounds the wait
      # when a burst exceeds the pool rather than failing immediately.
      maximum-pool-size: 12
      # Two rather than five. minimum-idle is held PER REPLICA, so five meant
      # fifteen connections sitting idle across a scaled-out deployment purely
      # to save a few milliseconds of connection setup inside the same cluster.
      minimum-idle: 2
      data-source-properties:
        # statement_timeout AND idle_in_transaction_session_timeout USED TO BE
        # HERE, as `options: "-c statement_timeout=30000 -c ..."`.
        #
        # They are still enforced, and still server-side, but they are now bound
        # to the ROLE instead of to the connection string:
        #
        #   ALTER ROLE core SET statement_timeout = '30s';
        #   ALTER ROLE core SET idle_in_transaction_session_timeout = '60s';
        #
        # applied by platform/envs/dev/database/session-settings.yaml. Same
        # values, same effect on every session core opens -- a change of
        # mechanism, not of policy.
        #
        # WHY IT MOVED (ADR-0012 section 5, T-2.18)
        #
        # `options` is part of the POOL KEY under a transaction-mode pooler.
        # Two clients with different options get different server pools, which
        # silently halves the multiplexing a pooler exists to provide, and some
        # PgBouncer versions reject the parameter outright. That failure arrives
        # when a pooler is added, looking like a pooler problem, months after
        # the line that caused it was written.
        #
        # Do not put session settings back here. Add them to the role.
        #
        # Client-side backstop for a connection that has gone away entirely, where
        # the server-side timeout can never fire because nothing is listening.
        # This is also what bounds the brief window during a deploy when a new
        # pod is up before the PostSync hook has run.
        socketTimeout: 45
        connectTimeout: 5
        tcpKeepAlive: true
  # Replace by 'prod, faker' to add the faker context and have sample data loaded in production
  flyway:
    enabled: true
    locations: classpath:db/migration
    # Fail rather than guess. Without this, Flyway will happily
    # "baseline" a database that already has tables it did not create,
    # silently adopting an unknown schema as version 1.
    baseline-on-migrate: false
    validate-on-migrate: true
  thymeleaf:
    cache: true

# ===================================================================
# To enable TLS in production, generate a certificate using:
# keytool -genkey -alias core -storetype PKCS12 -keyalg RSA -keysize 2048 -keystore keystore.p12 -validity 3650
#
# You can also use Let's Encrypt:
# See details in topic "Create a Java Keystore (.JKS) from Let's Encrypt Certificates" on https://maximilian-boehm.com/en-gb/blog
#
# Then, modify the server.ssl properties so your "server" configuration looks like:
#
# server:
#   port: 443
#   ssl:
#     key-store: classpath:config/tls/keystore.p12
#     key-store-password: password
#     key-store-type: PKCS12
#     key-alias: selfsigned
#     # The ciphers suite enforce the security by deactivating some old and deprecated SSL cipher, this list was tested against SSL Labs (https://www.ssllabs.com/ssltest/)
#     ciphers: TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA,TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA,TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256,TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384 ,TLS_DHE_RSA_WITH_AES_128_GCM_SHA256 ,TLS_DHE_RSA_WITH_AES_256_GCM_SHA384 ,TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA256,TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA384,TLS_DHE_RSA_WITH_AES_128_CBC_SHA256,TLS_DHE_RSA_WITH_AES_128_CBC_SHA,TLS_DHE_RSA_WITH_AES_256_CBC_SHA256,TLS_DHE_RSA_WITH_AES_256_CBC_SHA,TLS_RSA_WITH_AES_128_GCM_SHA256,TLS_RSA_WITH_AES_256_GCM_SHA384,TLS_RSA_WITH_AES_128_CBC_SHA256,TLS_RSA_WITH_AES_256_CBC_SHA256,TLS_RSA_WITH_AES_128_CBC_SHA,TLS_RSA_WITH_AES_256_CBC_SHA,TLS_DHE_RSA_WITH_CAMELLIA_256_CBC_SHA,TLS_RSA_WITH_CAMELLIA_256_CBC_SHA,TLS_DHE_RSA_WITH_CAMELLIA_128_CBC_SHA,TLS_RSA_WITH_CAMELLIA_128_CBC_SHA
# ===================================================================
server:
  port: 8081
  shutdown: graceful # see https://docs.spring.io/spring-boot/reference/web/graceful-shutdown.html#web.graceful-shutdown
  compression:
    enabled: true
    mime-types: text/html,text/xml,text/plain,text/css,application/javascript,application/json,image/svg+xml
    min-response-size: 1024

# ===================================================================
# JHipster specific properties
#
# Full reference is available at: https://www.jhipster.tech/common-application-properties/
# ===================================================================

jhipster:
  http:
    cache: # Used by the CachingHttpHeadersFilter
      timeToLiveInDays: 1461
  logging:
    use-json-format: false # By default, logs are not in Json format
    logstash: # Forward logs to logstash over a socket, used by LoggingConfiguration
      enabled: false
      host: localhost
      port: 5000
      ring-buffer-size: 512
# ===================================================================
# Application specific properties
# Add your own application properties here, see the ApplicationProperties class
# to have type-safe configuration, like in the JHipsterProperties above
#
# More documentation is available at:
# https://www.jhipster.tech/common-application-properties/
# ===================================================================

# application:

application:
  storage:
    # Hetzner Object Storage. Bucket and endpoint come from the environment so the
    # same image runs in staging and prod without a rebuild; there is deliberately
    # no default, because a wrong default here writes documents to another
    # environment's bucket and nothing would report an error.
    bucket: ${DOCUMENTS_BUCKET}
    endpoint: ${DOCUMENTS_ENDPOINT}
