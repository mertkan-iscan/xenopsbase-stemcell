<?xml version="1.0" encoding="UTF-8"?>
<!--
    core: the servlet service.

    WHAT USED TO BE HERE AND IS NOT ANY MORE. This file was 1464 lines, and
    about 1000 of them were the same 1000 lines as the gateway's pom: plugin
    versions, the whole pluginManagement block, the coverage floors, the two
    CVE version overrides, the profiles. All of that moved to ../pom.xml
    unchanged. What is left is what is actually true of THIS module and no
    other.

    Read ../pom.xml first if you are wondering where a plugin's configuration
    went.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.xenopsoftware</groupId>
        <artifactId>services</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <groupId>com.xenopsoftware.core</groupId>
    <artifactId>core</artifactId>
    <packaging>jar</packaging>
    <name>Core</name>
    <description>The servlet service: persistence, object storage, the outbox</description>

    <properties>
        <start-class>com.xenopsoftware.core.CoreApp</start-class>

        <!--
            LOWERED, AND THIS IS A RE-MEASUREMENT RATHER THAN A RELAXED STANDARD.
            Read the whole note before raising or reverting it.

            The floors were 0.80/0.70 and this module now measures 0.7800/0.5741
            on a merged unit+integration run. Nothing was untested to make that
            happen; classes LEFT. ADR-0017 moved the correlation filters, the
            exception translator, SecurityUtils, the idempotency filter, the
            auditing superclass and the tenancy resolver into
            platform-common-web, and the outbox and object storage into
            platform-common. Those were among the best-covered code here —
            CorrelationIdFilter alone was 26 branches of 26, deliberately, because
            it sanitises an attacker-controlled header that lands in every log
            line. Take 26 fully-covered branches out of a bundle with 134 and the
            ratio falls by more than a tenth without a single test being deleted.

            The tests moved with the code and pass in their new modules. What did
            NOT move is core's INTEGRATION tests, which is the part worth knowing
            about: DocumentResourceIT and IdempotencyFilterIT exercise the shared
            classes at runtime, but they run in this module, so that coverage is
            counted against core's bundle and not against the library's. The
            libraries' own floors (0.26/0.34 and 0.49/0.62, in their poms) are
            therefore lower than the code is actually exercised, and raising them
            honestly means writing unit tests down there rather than relying on a
            service's ITs. That is a follow-up with a number in it, not a guess.

            Measured on this machine, 2026-09-06, one point below on instruction
            and one on branch, same convention as every other floor here: the
            build fails on a REGRESSION today. Take the next ratchet from CI's
            job summary, not from a developer machine — services.yml prints the
            merged figure precisely so the number does not have to be guessed.

            RAISED THE SAME DAY, 0.77/0.56 to 0.80/0.59, after T-9.2. The move
            was not new tests either: deleting the demo domain took
            ExampleItemResource and its repository with it, and the probe that
            replaced Document is exercised by more of its own suite than Document
            was — the outbox write on both the create and delete paths is now on
            a path PlatformProbeResourceIT drives. Measured 0.8131 / 0.6019.
        -->
        <jacoco.minimum.instruction>0.80</jacoco.minimum.instruction>
        <jacoco.minimum.branch>0.59</jacoco.minimum.branch>
    </properties>


    <dependencies>
        <!--
            The servlet half of the platform, and `platform-common` with it
            transitively. Everything this service used to declare in
            com.xenopsoftware.core.{security,config,web.filter,web.rest.errors,
            tenancy,domain} arrives through this dependency instead, wired by
            auto-configuration rather than by a component scan (ADR-0017).
        -->
        <dependency>
            <groupId>com.xenopsoftware</groupId>
            <artifactId>platform-common-web</artifactId>
        </dependency>
        <dependency>
            <groupId>tech.jhipster</groupId>
            <artifactId>jhipster-framework</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-h2console</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-jackson2</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <!--
            Distributed tracing, exported over OTLP (T-2.7).

            Replaces a generated `zipkin` Maven profile that carried the Brave
            bridge and a zipkin reporter. That profile was never activated and
            the matching config pointed at http://localhost:9411, so the
            services produced no spans at all, while Tempo and Alloy were
            deployed, healthy, and receiving nothing. Tempo's own API confirmed
            it: {"tagNames":[]}, {"traces":[]}.

            Declared unconditionally rather than behind a profile for exactly
            that reason. A profile is a switch someone has to remember, and the
            failure when they do not is silence.

            The OTEL bridge rather than Brave because the collector speaks OTLP;
            Brave would mean translating a trace format for no benefit.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-opentelemetry</artifactId>
        </dependency>
        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-exporter-otlp</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aspectj</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <!--
            Valkey as a business cache (T-3.22, #264; ADR-0011).

            `spring-boot-starter-cache` brings the abstraction; `-data-redis` brings Lettuce, which
            speaks the Valkey wire protocol. The blocking client, not the reactive one: `core` is a
            servlet application and the gateway's reactive dependency is for sessions, not this.

            NOT `spring-boot-starter-session-data-redis`. Sessions belong to the gateway and live in
            a different Valkey instance (T-2.19, #262); pulling session support in here would have
            `core` writing to a store it does not own.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-cache</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jackson</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jackson-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-mail</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-client</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-restclient</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!--
            The JPA slice (T-5.2).

            Boot 4 split the test slices into per-technology modules, and
            spring-boot-starter-test brings only some of them. @WebMvcTest
            resolves without help because spring-boot-webmvc-test arrives
            transitively; @DataJpaTest and @AutoConfigureTestDatabase do not.
            The failure is a package-does-not-exist on an annotation whose name
            is unchanged from Boot 3, so it reads like a typo rather than a
            missing dependency.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-data-jpa-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-jdbc-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-tomcat</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-data</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-api</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-hppc</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
        </dependency>
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
        </dependency>
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5-api</artifactId>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.slf4j</groupId>
                    <artifactId>slf4j-api</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5-engine</artifactId>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.slf4j</groupId>
                    <artifactId>slf4j-api</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>com.zaxxer</groupId>
            <artifactId>HikariCP</artifactId>
        </dependency>
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-java</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-junit-platform-engine</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-spring</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
        </dependency>
        <dependency>
            <groupId>org.glassfish.jaxb</groupId>
            <artifactId>jaxb-runtime</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.hibernate.orm</groupId>
            <artifactId>hibernate-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.hibernate.orm</groupId>
            <artifactId>hibernate-processor</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.hibernate.validator</groupId>
            <artifactId>hibernate-validator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.junit.platform</groupId>
            <artifactId>junit-platform-suite</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
            <version>${mapstruct.version}</version>
        </dependency>
        <dependency>
            <groupId>org.openapitools</groupId>
            <artifactId>jackson-databind-nullable</artifactId>
            <version>${jackson-databind-nullable.version}</version>
        </dependency>
        <dependency>
            <!-- Spring Boot 4 moved autoconfiguration out of the monolithic
                 spring-boot-autoconfigure into per-module artifacts. Without
                 this, flyway-core sits on the classpath and is NEVER RUN: no
                 error, no log line, and the first symptom is Hibernate's
                 "Schema validation: missing table" for a table the migration
                 would have created. -->
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-flyway</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <!-- Flyway needs the Postgres-specific module from 10.x onward;
                 without it startup fails with "Unsupported Database: PostgreSQL". -->
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-circuitbreaker-resilience4j</artifactId>
        </dependency>
        <!--
            The sync transport, declared explicitly rather than inherited as a runtime
            transitive. Two reasons: StorageConfiguration configures its connect and socket
            timeouts by name (T-3.9), which needs it at compile scope; and leaving the transport
            implicit means an SDK upgrade can swap it without anything in this project changing,
            taking the timeout configuration with it.
        -->
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>apache5-client</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>s3</artifactId>
            <exclusions>
                <!--
                    The sync client uses apache-client; netty comes in only for the
                    async one, which this service does not use. Excluding it keeps a
                    second HTTP stack and its native transports out of the image.
                -->
                <exclusion>
                    <groupId>software.amazon.awssdk</groupId>
                    <artifactId>netty-nio-client</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-minio</artifactId>
            <!--
                Not managed by the Spring Boot BOM, which covers only some
                Testcontainers modules. Pinned to the same version the BOM manages for
                the core artifact so the two cannot drift apart.
            -->
            <version>${testcontainers.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-jdbc</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-postgresql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>tools.jackson.datatype</groupId>
            <artifactId>jackson-datatype-hibernate7</artifactId>
        </dependency>
        <dependency>
            <groupId>tools.jackson.module</groupId>
            <artifactId>jackson-module-jaxb-annotations</artifactId>
        </dependency>
    </dependencies>

    <!--
        The plugins that turn this module into a deployable service. Their
        versions and configuration are in ../pom.xml's pluginManagement, which
        is why nothing is stated about them here; the parent's own <plugins>
        list carries the ones every module runs, libraries included.
    -->
    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>com.google.cloud.tools</groupId>
                <artifactId>jib-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>properties-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.openapitools</groupId>
                <artifactId>openapi-generator-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.sonarsource.scanner.maven</groupId>
                <artifactId>sonar-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
