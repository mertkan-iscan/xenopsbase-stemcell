# Copy to <environment>.secrets.tfvars and fill in. Those files are gitignored.
#
# The API token is NOT here. Supply it via TF_VAR_cloudflare_api_token.

account_id = "" # Cloudflare dashboard sidebar
zone_id    = "" # zone overview page for xenopsoftware.com
