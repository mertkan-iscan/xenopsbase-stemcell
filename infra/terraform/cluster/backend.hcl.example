# Copy to backend.hcl and fill in. backend.hcl is gitignored.
#
#   cp backend.hcl.example backend.hcl
#   terraform init -backend-config=backend.hcl
#
# ⚠️ This points at CLOUDFLARE R2, not Hetzner. See ADR-0005.
#
# Credentials for this backend are the R2 ones:
#   export AWS_ACCESS_KEY_ID=<r2 access key id>
#   export AWS_SECRET_ACCESS_KEY=<r2 secret access key>

bucket = "xenopsbase-tfstate"

# The state key is NOT set here. It is derived from ENV by the Makefile and
# passed as -backend-config="key=..." on every init, together with -reconfigure.
# That is what makes it impossible to point one environment's variables at
# another environment's state (T-1.4).


# R2 has no regions. "auto" is the required literal.
region = "auto"

endpoints = {
  s3 = "https://<account_id>.r2.cloudflarestorage.com"
}
