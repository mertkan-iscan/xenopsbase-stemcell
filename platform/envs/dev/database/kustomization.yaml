apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Order matters at apply time as well as at sync time: the Cluster references
# the ObjectStore by name, so the ObjectStore is listed first.
resources:
  - objectstore.yaml
  - cluster.yaml
