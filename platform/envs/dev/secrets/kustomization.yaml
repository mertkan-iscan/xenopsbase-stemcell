apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Decrypted by ksops at render time, never committed in plaintext, and never
# stored decrypted anywhere.
generators:
  - secret-generator.yaml
